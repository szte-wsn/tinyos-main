COMPONENT=NullAppC
CFLAGS+=-Istm25p/
CFLAGS+=-Ideluge/
# CFLAGS+=-I${TOSDIR}/lib/net/Deluge
# CFLAGS+=-I${TOSDIR}/lib/tosboot/ucmini
CFLAGS+=-DPLATFORM_BAUDRATE=57600UL
LDFLAGS = -Ttext=0x1E000
CFLAGS+=-DBOOTLOADER_INTERRUPTS
CFLAGS+=-fno-jump-tables
CFLAGS+=-mrelax
CFLAGS+=-DATMEL_NO_PROGMEM

CFLAGS+=-DDISABLE_SERIAL_AUTO
CFLAGS+=-DDISABLE_SERIAL_RESET
CFLAGS+=-DDISABLE_BATTERY_CHECK

CFLAGS+=-DAVR109_HW_MAJOR=\'2\'
CFLAGS+=-DAVR109_HW_MINOR=\'1\'

CFLAGS+=-DBOOTLOADER_MAGIC_PTR=RAMEND-3
CFLAGS+=-DBOOTLOADER_MAGIC_VALUE=12345

#change the main oscillator to transciever oscillator
#PROGRAMMER_EXTRA_FLAGS=-Ulfuse:w:0xef:m
#change the main oscillator to RC oscillator
#PROGRAMMER_EXTRA_FLAGS=-Ulfuse:w:0xe2:m

TINYOS_ROOT_DIR?=../..
include $(TINYOS_ROOT_DIR)/Makefile.include